Testing the muSim controller
============================

To test the trained muSim controller, run:

   ``python main.py --config configs/configs.txt --mode test --visualize True``

This will visualize the xml model performing movements for training and testing conditions using the trained muSim controller. 

This will also save the files used for post-training *qualitative* and *quantitative* analyses modules.
